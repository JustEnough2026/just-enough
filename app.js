const {store,products}=window.STORE_CONFIG;const state={};const money=n=>`$${n.toLocaleString('zh-TW')}`;
document.querySelector('#announcement').textContent=store.announcement;document.querySelector('#open-status').textContent=store.open?`今日營業 ${store.hours}`:'今日店休';
[['line-link',store.lineCommunity],['map-link',store.googleMaps],['foodpanda-link',store.foodpanda],['uber-link',store.uberEats]].forEach(([id,u])=>{let e=document.getElementById(id);if(u)e.href=u;else e.style.display='none'});document.querySelector('#phone-link').href=`tel:${store.phone}`;document.querySelector('#info-phone').textContent=store.phoneDisplay;document.querySelector('#info-phone').href=`tel:${store.phone}`;document.querySelector('#info-address').textContent=store.address;
const list=document.querySelector('#product-list');products.forEach(p=>{state[p.id]=0;const d=document.createElement('article');d.className='product';d.innerHTML=`${p.badge?`<span class="badge">${p.badge}</span>`:''}<img src="${p.image}" alt="${p.name}"><h3>${p.name}</h3><div>${p.desc||''}</div><div class="price">${money(p.price)}</div>${p.options?`<div class="opts">${p.options.map(x=>`<span class="opt">${x}</span>`).join('')}</div>`:''}<div class="qty"><button data-d="-1">−</button><b>0</b><button data-d="1">＋</button></div>`;d.querySelectorAll('button').forEach(b=>b.onclick=()=>{state[p.id]=Math.max(0,state[p.id]+Number(b.dataset.d));d.querySelector('b').textContent=state[p.id];render()});list.appendChild(d)});
function render(){let c=0,t=0;products.forEach(p=>{c+=state[p.id];t+=state[p.id]*p.price});document.querySelector('#cart-count').textContent=`${c} 份`;document.querySelector('#cart-total').textContent=money(t)}
